<% if @transfer.errors.any? %>
	$('#error_explanation').dialog({
			modal: true,
			buttons: {
				Ok: function() {
					$( this ).dialog( "close" );
				}
			}
		});
<% else %>
	$('#dialog').dialog('close');
<% end %>