<%= form_for(@earning, :remote => true) do |f| %>
  <% if @earning.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@earning.errors.count, "error") %> prohibited this earning from being saved:</h2>

      <ul>
      <% @earning.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name, :class => 'text ui-widget-content ui-corner-all' %>
  </div>
  
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description, :class => 'text ui-widget-content ui-corner-all' %>
  </div>
  
  <div class="field">
    <%= f.label :value %><br />
    <%= f.text_field :value, :class => 'text ui-widget-content ui-corner-all' %>
  </div>
  
  <div class="field">
    <%= f.label :date %><br />
    <%= f.text_field :date, :class => 'text ui-widget-content ui-corner-all' %>
  </div>  
  
  <div class="actions">
    <%= f.submit :class => 'submitButton' %>
  </div>
<% end %>