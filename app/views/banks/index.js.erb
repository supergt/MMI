$('#dialog').html("<%= escape_javascript(render :partial => 'show') %>")
	.dialog('option', 'title', 'All Banks')
	.dialog('open');

	$('.ui-state-default').hover(
	function(){ $(this).addClass('ui-state-hover'); },
	function(){ $(this).removeClass('ui-state-hover'); }
);